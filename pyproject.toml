[project]
name = "sem4"
version = "0.1.0"
description = "A Qt 5 desktop agent that chats with the Cerebras API and runs file and shell tools."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cerebras-cloud-sdk>=1.91.0",
    "pyqt5==5.15.11",
    "pyqt5-qt5==5.15.2",
    "python-dotenv>=1.2.3",
]
